#ifndefine BALANCE_H
#define BALANCE_H

class M {
        
public:

  M(){}

 ~M(){}
        
	void a(int);
	void b(string);   
	void c(string);
	void d();
	void e();
	void f();
	void g();
	void h();
	void i();
	void j();
	void k();
	void l();
	void m();
	
protected:

				static short choice;//choice to buy (1), sell (-1), or hold (0)	


				pair<string, int> gPair;
				pair<string, int> vPair;
				pair<string, int> sPair;
				 
				static int N;//how many runs
				static int g;//ngene tolerance 
				
				static string y;
				static string z;
				static string nm;
				static vt<double> px;
				
 			static set<string> n;
	
				static vt<vt<double> >vv;
				
				static vt<double> v;
 			static vt<double> d;
 			
				static map<string, double> mn;

				static map<string, vt<double> > m;

				static map<string, int> c;
				
				static map<int, set<string> xn;
				
				static map<string, pair<set<int>, vt<double> > >t;
			
				static map<double, map<string, pair<set<string, vt<double> > > gmap;
				static map<double, map<string, pair<set<string, vt<double> > > vmap;
				static map<double, map<string, pair<set<string, vt<double> > > smap;
				
				static set< pair<string, int> > whereFit;
				
				static map<double, pair<set<string>, vt<double> > > fitmap;
				
				static map<string, set<pair<int, double> > >fitrows;
				
				static map<string, pair<double, double> > nn;
			 
			 static map<string, set<int> >msi;
							
				static map<string, vt<double> >msv;
			
				static map<string, pair<set<int>, vt<double> > > uu;

				static map<string, map<int, pair< set<string>, vt<double> > > >one;

				static string sfx1;
				static string sfx2;
				static string sfx3;
							
				static set<string> sfx;
							
				sfx.in(sfx1); 
				sfx.in(sfx2);
				sfx.in(sfx3);
										
				static string g;
				static string s;
				static string v;
				
				static set<string> typ;
     
};

#endif

